# 2026.04.23
要求：新开发加入这些任务功能最好需要是单独的py文件脚本，不要和之前的那些脚本工具耦合在一起，不然改的话不太好改，要大片改。
1. 任务1：融合定位结果的连续性判断
大概的需求：判断出连续缺失/中断的imu和veh数据导致的pvtlc、rtklc融合定位数据缺失和中断
- topic_parse解析目录下面的imu.csv和vehicle.csv是从要处理的原始的record文件中提取出来的imu和轮速数据
- 时间戳对齐用GPS秒进行判断，可以在topic_parse解析record文件时，给imu.csv和vehicle.csv的最后一列添加上GPS秒(具体怎么读取和给这两个文件加上GPS秒一列，可以从topic_parse文件夹下面的pvt.csv和gnss.csv的measure_time字段去获取，这个measure_time字段下面就是GPS秒)，要去和pcvtlc_xxxxx和rtklc_xxxxx文件下的msf_debug_state.csv的最后一列(也是GPS秒字段，也可以用来判断时间戳的连续关系)做对比，如果原始解析的imu.csv和vehicle.csv中存在大段缺失(时间戳严重不连续)，就需要去msf_debug_state.csv文件中去找到这一段，。(这一段我没有讲很清楚，可能还需要ni根据这些csv文件中的具体值去细化构思一下)。
  - 时间戳肯定不会完全相同，不用特别准，大概准就行，因为频率不会完全一直稳定的；比如：轮速 imu数据正常都是100hz输出，输出的结果msf_debug_state应该是5hz输出的，需要按照频率去判断
  - 很短的时间戳缺失就不用管(1s或者几秒范围内？可以做个配置选项设置在toml文件?)，大段的缺失就需要标记出来
  - 大段缺失的话，那就将这一段的评估结果在绘图的时候(绘制error误差图的时候)置为10或者5，固定数字就行；大段缺失的这个时间戳区间段的左右边界还是要和左和右的正常区域连接起来的
- 根据这些csv文件进行下测试，测试你加的功能
  - 这是示例csv文件：topic_parse目录下的imu vehicle gnss pvt，pvtlc_c80c32da下的msf_debug_state.csv文件
- 最后要实现的功能目标：
  - 在最终绘制的误差曲线图error_analysis_type_All_with_gnss.png、error_analysis_type_All_without_gnss.png等所有的误差曲线图中，将这些大段缺失时间段置出来，然后单独输出一个txt文件，写上缺失的时间段的GPS秒的范围
  - 输出的最终的子数据集文件夹(例如：20260410是总数据集目录，20260410/2026-04-10_14-58-57/results/是子数据集结果目录)的和聚合的精度文件(在20260410/下面)，统计各个指标的的时候也不要带上这些大段缺失的时间段
2. 任务2：融合定位指定版本优劣对比（包括可视化）
还没想好怎么做，优先级不是很高，你可以根据脚本工具给点思路和建议;
- 单独的脚本py来做
- 现在的脚本的评估结果已经在云端上存着了，无需去重新再跑一边，直接拿现有的评估结果数据来做就可以了
- 优劣对比直接对比统计结果，通过图的方式展示出来，聚合精度的结果和子数据集的结果都整出来做来 

3. 任务3：统计指标的可视化
统计指标rms\cep95等指标的可视化

4. 需求加入：在toml文件中选择压缩或者不压缩数据，有时重跑时并不需要压缩数据

# 2026.04.24
1. 在最后要进行压缩的时候加入一个对话判断，询问本次数据处理是否需要压缩数据，由用户输出y或者n来决定。（因为反复验证脚本工具的时候会重复压缩数据，导致占用云端内存空间）
2. 是不是也需要检测一下pvtlc和rtklc的msf_debug_state.csv和topic_parse的imu.csv和vehicle是不是时间戳连续啊？如果它们本身是时间戳就缺失很多，那么即使比较了它们对应的GPS秒，也会漏掉时间戳本身就不连续的区段。不知道这个功能加进来了没有？
  
---
3. 这是刚刚生成的数据集的误差png文件，阴影部分是表示出来了，但是这几个最大值还是有点离谱，而且gap的左右边界的连线还是连接上了的，我想要的效果是左边界不要连接右边界，中间只保留灰色区域即可，我感觉应该是因为gap时间段的左右边界的值很大，然后导致刚恢复时不能完全进入正常状态，是否应该加入一个逻辑，gap的左右边界的两边各扩展1秒？确保恢复正常？或者你还有什么更好的想法吗？