# 测试配置：单个数据集 0424-下午 正常
# 可用方案: lc (pvtlc), tc (rtklc), gnss
input_dir = "/Users/ading/Code_Projects/Deep_Learning_for_Studying/automated_evaluation_pys/示例子数据集部分结果/0424-下午 正常"
version_label = "c80c32da_0424"

# 要可视化的方案（留空则全部显示）
# 可选: ["lc"], ["tc"], ["lc", "tc"], ["lc", "tc", "gnss"]
visualize_schemes = ["lc", ]

# 要可视化的场景（留空则自动检测）
visualize_scenes = []

# 可视化类型
# bar = 每个指标生成独立的条形图
# table = 文本汇总表
visualize_types = ["bar", "table"]

# 输出基础目录
output_base_dir = "/Users/ading/Code_Projects/Deep_Learning_for_Studying/automated_evaluation_pys/示例子数据集部分结果"
