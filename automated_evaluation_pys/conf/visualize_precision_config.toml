# 测试配置：单个数据集 0424-下午 正常
# 可用方案: lc (pvtlc), tc (rtklc), gnss
# 云端路径: /mnt/data/oss-byd-wl-roadtest/users/localization/evaluation/ota630_c80c32da_0424
input_dir = "/mnt/data/oss-byd-wl-roadtest/users/localization/evaluation/ota630_c80c32da_0424"
version_label = "c80c32da_0424"

# 要可视化的方案（留空则全部显示）
# 可选: ["lc"], ["tc"], ["lc", "tc"], ["lc", "tc", "gnss"]
visualize_schemes = ["lc", "tc"]

# 要可视化的场景（留空则自动检测并按标准顺序排列）
# 标准顺序: 全部, 正常, 开阔场景, 半遮挡, 双边遮挡, 隧道, 转发器
visualize_scenes = []

# 可视化类型
# bar = 每个指标生成独立的条形图
# table = 文本汇总表
visualize_types = ["bar", "table"]

# 输出基础目录
output_base_dir = "/mnt/data/oss-byd-wl-roadtest/users/localization/evaluation"
