现在我需要做一个单独的本地工具，
这段话是组长给我发的需求：
“需要做一下，初始化成功率/耗时测试。使用非静态带卫星定位的数据。1. 耗时测试用随机切片的方法来测，就是随机灌入一段数据，统计一下耗时。2. 成功率测试，用一些难例数据做随机切片，可以灌长一点，看是否能成功初始化。”
然后，和我同事进行沟通后，大致确定了一下实现方法。

注意：因为本地存储有限，所以现在只是在本地环境下去测试和运行，后续需要用云端去跑，云端可能就不是docker环境了，而是直接用TCMSF的bin文件去跑，不过，现阶段，还是先把本地的链路跑通再说云端怎么跑吧

前提条件：
1.本地回灌的方式：命令行
‵‵‵bash
docker start byd_ird_dingxingyu
docker exec -it byd_ird_dingxingyu /bin/bash
source scripts/cyber_setup.bash
export CYBER_PATH=/apollo/cyber_release/dist/x86_64
cd /apollo && \
bazel clean --async && \
bazel build modules/localization/src/TCMSF:TCMSF --config=release_build && \
clear && \
./bazel-bin/modules/localization/src/TCMSF/TCMSF \
  data/2026/data_0410-145857 \   //这里是本地数据集的存储方式，本地的/home/dingxingyu/E2E_PingCe/ota_data已经挂载到了docker容器下面的/apollo/data文件夹，两者同源
  data/tcmsf_output \   //只是一个需要设置的结果输出路径，实际的结果输出在/apollo/data/tmp下面，
  2>&1 | tee /apollo/data/log/tcmsf_$(date +%Y%m%d_%H%M%S).log  // 输出的日志log，位置路径如其所示
```

初版需求的执行方案探讨：
1.组长说通过日志文件来探讨确定初始化的耗时时间，目前的本地定位代码位置在/home/dingxingyu/E2E_PingCe/localization，下面是示例的log日志文件，
组长说：“可以参考日志里面这几个字段。dynamic ready是运动状态满足，start init是多帧校验成功开始初始化，Ready是完成初始化。”
```
imu_processor.cpp:179][startup]Dynamic ready(gnss)!imu time: 1774324325.0291
imu_processor.cpp:329][startup] TCMSF Start Init! imu time: 1774324325.5280
tcmsf interface_impl.cpp:1258][startup]TCMSF Ready! imu time: 1774324326.5375
```
和另一位有资历的同事讨论后（就参考他的方法），说：不用日志log文件，而是在程序里找到是在哪打印的(/home/dingxingyu/E2E_PingCe/localization/TCMSF/tcmsf/processor/src/imu_processor.cpp，/home/dingxingyu/E2E_PingCe/localization/TCMSF/interface/src/tcmsf_interface_impl.cpp)？把这个时间点状态记录下来，新生成一个文件，如果输出并且有这个文件，就可以算是初始化成功的。

因为还需要统计耗时，所以可以先在这个文件里面统计计算下耗时？在文件最后给出一个结论：是否初始化成功？以及耗时多久？

2.跳过 非静态卫星定位数据：可以通过输入的Gnss的速度进行判断，判断代码在：/home/dingxingyu/E2E_PingCe/localization/TCMSF/tcmsf_main.cc中的220行左右“if (msg_.channel_name == GPS_TPC ...."这里（具体的你判断一下是不是这里），速度为0就是静态，否则就是非静态。（可能还要加其他约束东西？待定）

3.随机切片的做法：这位同事说不用随机函数的做法，有点复杂，可以这样做：在/home/dingxingyu/E2E_PingCe/localization/TCMSF/tcmsf_main.cc的main()函数那里，加上unix时间戳参数？"tcmsf.run(argv[1], argv[2]);"(大概是这里)，
大概思路就是：给个起点时间，做个循环(循环record，从不同的时间戳所在的record开始做初始化耗时和成功率的统计)，每次累加一个固定时间(可以先设置为40s)，然后跳过这个固定的秒数，从那个时间戳位置开始去进行初始化，实现随机切片去进行初始化的效果。
时间戳的消息处理是在：/home/dingxingyu/E2E_PingCe/localization/TCMSF/tcmsf_main.cc  run()函数下面，208(while (reader.ReadMessage(&msg_)) ),这是循环读取消息入口，可以用/home/dingxingyu/E2E_PingCe/localization/TCMSF/interface/src/tcmsf_interface_impl.cpp第763行"veh_msg_->header().has_measurement_timestamp() &&“的测量时间来跳过一定间隔的record数据?

4.后续我手上有一个toml文件，里面装了各个场景(开阔 半遮挡 双边遮挡 转发器 隧道等场景)的对应时间戳范围，要加上"难例数据"(半遮挡 双边遮挡 转发器)场景做其初始化是否成功和耗时的统计，暂时先不加这个功能。

5.组长还说了其他内容(是否可以暂时不用考虑)：
(1) 单个record没完成初始化的，可以多灌几个record。有的record可能一直卫星状态太差。
(2) 统计下有没有这句日志。有这句的话，说明误差很大做了重置:
/home/dingxingyu/E2E_PingCe/localization/TCMSF/tcmsf/processor/src/gps_single_processor.cpp第71行：" AERROR << "reinit due to large pos error";


现在，根据你的知识库和你的推理能力，以及我提供给你的内容(只是想法，可能存在偏差，需要你认真仔细的去判断)，去生成一个初步版本的独立工具，用来进行初始化的耗时统计和成功率统计。